export const fonts = {
  sans: "var(--font-uncut-sans), system-ui, -apple-system, sans-serif",
  mono: "var(--font-paper-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace",
  serif: "var(--font-tabular), system-ui, sans-serif",
} as const;
